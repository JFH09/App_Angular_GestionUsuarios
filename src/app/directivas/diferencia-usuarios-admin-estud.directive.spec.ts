import { DiferenciaUsuariosAdminEstudDirective } from './diferencia-usuarios-admin-estud.directive';

describe('DiferenciaUsuariosAdminEstudDirective', () => {
  it('should create an instance', () => {
    const directive = new DiferenciaUsuariosAdminEstudDirective();
    expect(directive).toBeTruthy();
  });
});
