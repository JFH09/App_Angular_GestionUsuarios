import { CabecerasTamano20Directive } from './cabeceras-tamano20.directive';

describe('CabecerasTamano20Directive', () => {
  it('should create an instance', () => {
    const directive = new CabecerasTamano20Directive();
    expect(directive).toBeTruthy();
  });
});
