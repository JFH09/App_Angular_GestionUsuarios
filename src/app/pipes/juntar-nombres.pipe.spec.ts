import { JuntarNombresPipe } from './juntar-nombres.pipe';

describe('JuntarNombresPipe', () => {
  it('create an instance', () => {
    const pipe = new JuntarNombresPipe();
    expect(pipe).toBeTruthy();
  });
});
